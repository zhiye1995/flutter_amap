# flutter_amap_navi 独立示例

此工程只依赖 `flutter_amap_navi`，用于验证导航与巡航能力不依赖地图包。

```shell
flutter run --dart-define=AMAP_ANDROID_KEY=your-android-key --dart-define=AMAP_IOS_KEY=your-ios-key
```

启动导航前需在系统中授予定位权限。后台导航还需按商店政策和业务需要配置后台定位能力。
