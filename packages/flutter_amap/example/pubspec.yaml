name: flutter_amap_standalone_example
description: Demonstrates the standalone flutter_amap map plugin.
publish_to: 'none'
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_amap: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
